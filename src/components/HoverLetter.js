function HoverLetter({letter}) {
  return (
    <span class="text-customblue font-extrabold hover:text-customwhite transition-color ease-out duration-1000">
        {letter}
  </span>
  );
}

export default HoverLetter;
